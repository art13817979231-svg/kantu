# Changelog

## 1.0.0 — 2026-05-21

### 新增

- **文本标注**：双击画布创建/编辑，背景色预设，随项目保存
- **群组框**：四角缩放、框色预设、移动成员自动贴合
- **未命名项目自动保存**：桌面版在尚未「另存为」时，每 2 分钟写入应用数据目录 `drafts/`
- **自动保存恢复**：启动时可恢复最近项目的 `.autosave` 与未命名草稿

### 修复

- 删除群组框时同步解除框内**文本**的 `groupId` 绑定
- 删除框内成员后自动 `syncFrameBounds`，框大小重新贴合内容
- 手动缩放群组框与自动贴合逻辑并存（移动内部图片仍会更新框）

### 开发

- 新增 `npm test`（Vitest）覆盖群组解绑、迁移、autosave 路径等
- 版本号统一为 **1.0.0**（`package.json` / Tauri / Cargo）

### 已知限制

- 无撤销/重做
- 与官方 PureRef 的 `.pur` 格式不兼容
- 图层侧栏暂以图片为主，文本未完全纳入
- macOS/Windows 安装包需自行代码签名后分发
